{
  "name": "mancala-kalah-online",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "wrangler dev --port 3000",
    "dev:node": "node --watch src/server.js",
    "dev:cf": "wrangler dev --port 3000",
    "deploy:cf": "wrangler deploy",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
